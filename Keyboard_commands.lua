-- Decompiled with the Synapse X Luau decompiler.

local v1 = require(script.Parent:WaitForChild("BaseCharacter"));
local 

-- pressed button commands--
while wait() do
    if require(game.ReplicatedStorage.ModuleScript).is_key_just_pressed("o") then
        
    end
end
while wait() do
    if require(game.ReplicatedStorage.ModuleScript).is_key_just_pressed("i") then
        
    end
end
